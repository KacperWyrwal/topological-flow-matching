# Hydra configuration
run:
  dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
sweep:
  dir: multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
  subdir: ${hydra.job.num}
job:
  chdir: true

# Example Optuna sweeper config (enable by overriding hydra/sweeper)
# sweeper:
#   _target_: hydra_plugins.hydra_optuna_sweeper.optuna_sweeper.OptunaSweeper
#   direction: minimize
#   n_trials: 20
#   n_jobs: 1
#   params:
#     trainer.lr: interval(1e-5,1e-3)
#     sde.c: interval(0.01, 1.0)


